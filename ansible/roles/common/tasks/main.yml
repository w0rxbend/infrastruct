---
- name: Report common baseline placeholder
  ansible.builtin.debug:
    msg:
      - "common role is a placeholder."
      - >-
        Future tasks should manage hostname expectations, time sync, base
        OS settings, and ARM-specific prerequisites.
