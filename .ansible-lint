---
profile: production
offline: true

exclude_paths:
  - .cache/

warn_list:
  # Keep experimental rules visible without making the current scaffold depend
  # on ansible-lint behavior that may change between workstation installs.
  - experimental
