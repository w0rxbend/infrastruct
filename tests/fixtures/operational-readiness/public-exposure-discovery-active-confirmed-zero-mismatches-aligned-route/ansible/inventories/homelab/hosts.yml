---
all:
  hosts:
    edge-01:
      public_exposure:
        services:
          - route_id: demo-web
            runtime: Docker Compose
            protocol: https
            proxy_owner: Caddy
            public_host: demo.example.invalid
            target_host_or_cluster: edge-01
            target: demo-web:8080
            firewall_intent: allow from internet
            secret_dependency: none
            review_notes: Fixture route for validator tests.
  children:
    public_exposed:
      hosts:
        edge-01: {}
