---
all:
  hosts:
    edge-01:
      public_exposure:
        services:
          - route_id: active-inventory-only
            exposure_state: active
            runtime: Docker Compose
            protocol: https
            proxy_owner: Caddy
            public_host: active.example.invalid
            target_host_or_cluster: edge-01
            target: active-service:8080
            firewall_intent: allow from internet
            secret_dependency: none
            review_notes: Active production route must align across all sources.
  children:
    public_exposed:
      hosts:
        edge-01: {}
